# v0.3.0-beta Operator Checklist

- Confirm branch is `v0.3-beta-strict-live-evidence` for the strict evidence pass.
- Resume from the Claude `01-op-stack-public-testnet` authentication blocker, then continue remaining required prompts.
- Run `make validate-v03-bootstrap`.
- Install or refresh the local plugin in the target platform.
- Run each required dogfood prompt.
- Capture transcript metadata JSON and redacted Markdown.
- Capture generated artifact bundles for non-refusal prompts.
- Validate each live-run package.
- Record hook observations for Codex and Claude.
- Run `make validate-v03`.
- Create `v0.3.0-beta` tag only after strict validation passes.
- Do not push, publish, deploy, create real secrets, add MCP servers, or create the mainnet approval marker.
