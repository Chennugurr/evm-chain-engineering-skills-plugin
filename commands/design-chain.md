# Design Chain

Normalize the request, refuse unsafe secret handling, select expected skills, compare stacks, and produce the complete artifact bundle contract: request, assumptions, ADR, stack matrix, chain spec YAML, infra plan, security review, runbook, launch gates, validation report, and open questions.
