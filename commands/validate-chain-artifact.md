# Validate Chain Artifact

Run or recommend `python3 scripts/validate_artifact_bundle.py --strict --path <bundle>` and `python3 scripts/validate_generated_configs.py --strict --path <bundle>`, then summarize findings.
