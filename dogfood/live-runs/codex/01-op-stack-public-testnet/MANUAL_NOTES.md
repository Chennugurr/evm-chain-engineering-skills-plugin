# Codex Manual Notes

- Codex marketplace `evm-chain-engineering` was added from the local repository.
- Codex plugin `evm-chain-engineering-pro@evm-chain-engineering` was installed before the run.
- The Codex CLI emitted nonfatal curated-plugin sync warnings unrelated to this local plugin.
- The Codex process was terminated after the generated bundle and validation reports were already written because it remained alive in post-run refresh warnings.
- The committed stream is redacted. Raw tool outputs were removed from the committed evidence to keep strict safety scans at zero findings.
- No beta tag was created.
