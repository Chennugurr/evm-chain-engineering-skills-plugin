apiVersion: apps/v1
kind: Deployment
metadata:
  name: polygon-cdk-validium-enterprise-rpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: polygon-cdk-validium-enterprise-rpc
  template:
    metadata:
      labels:
        app: polygon-cdk-validium-enterprise-rpc
    spec:
      containers:
        - name: rpc-node
          image: example/polygon-cdk-rpc:v0.0.0-placeholder
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          env:
            - name: SIGNER_MODE
              value: external-signer-required
